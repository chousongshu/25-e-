# 小区电动车充电桩设备 — 项目框架文档

## 一、项目概述

基于 STM32F407ZG 的 32 路电动车充电桩控制系统。通过 RS485 与上位机通信，实现充电端口的远程控制、电流监测、计时计费、安全保护等功能。参数通过 SPI Flash 掉电保存，RTC 芯片提供时间基准。

### 核心功能

- 32 路继电器独立控制（开/关充电插座）
- 32 路电流检测（判断正常充电/涓流/设备移除/过流）
- 两种充电模式：自停模式（充满自停）、计时模式（定时关断）
- 涓流检测与涓流超时断电
- 设备移除检测（带防抖动）
- 过流保护
- 门禁报警、交流断电报警
- 外部看门狗防程序跑飞
- RTC 实时时钟
- 参数掉电保存（W25Q64 Flash）
- RS485 通信（CRC16 校验）

---

## 二、硬件资源

### MCU

- 芯片：STM32F407ZG
- 主频：168MHz
- Flash：1MB，RAM：192KB

### 引脚分配

```
┌─────────┬──────────┬──────────────────────────────────────┐
│ 引脚     │ 外设      │ 功能                                 │
├─────────┼──────────┼──────────────────────────────────────┤
│ PA0     │ ADC3_CH0 │ 74HC4067(U1)输出 → 1~16路电流采样    │
│ PA1     │ ADC3_CH1 │ 74HC4067(U2)输出 → 17~32路电流采样   │
│ PA2     │ GPIO_OUT │ 外部看门狗使能（低有效）               │
│ PA3     │ GPIO_OUT │ 外部看门狗喂狗（翻转）                 │
│ PA4     │ GPIO_IN  │ 交流断电检测（光耦，低=断电）          │
│ PA5     │ SPI1_SCK │ W25Q64 时钟                          │
│ PA6     │ SPI1_MISO│ W25Q64 数据输出                       │
│ PA7     │ SPI1_MOSI│ W25Q64 数据输入                       │
│ PA8     │ GPIO_OUT │ RS485 方向控制（0=接收，1=发送）       │
│ PA9     │ USART1_TX│ RS485 发送                            │
│ PA10    │ USART1_RX│ RS485 接收                            │
│ PA11    │ GPIO_OUT │ W25Q64 片选（低有效）                  │
├─────────┼──────────┼──────────────────────────────────────┤
│ PB0     │ GPIO_IN  │ 门禁按键（低=门被打开）                │
│ PB6     │ GPIO_OUT │ I2C SCL（软件模拟，连 BL8025）        │
│ PB7     │ GPIO_OUT │ I2C SDA（软件模拟，连 BL8025）        │
├─────────┼──────────┼──────────────────────────────────────┤
│ PC0~PC3 │ GPIO_OUT │ 74HC4067(U1)地址线 S0~S3             │
│ PC4~PC7 │ GPIO_OUT │ 74HC4067(U2)地址线 S0~S3             │
│ PC8     │ GPIO_OUT │ 74HC4067(U1)使能（低有效）            │
│ PC9     │ GPIO_OUT │ 74HC4067(U2)使能（低有效）            │
├─────────┼──────────┼──────────────────────────────────────┤
│ PD0~PD15│ GPIO_OUT │ 继电器17~32（推挽输出，高=吸合）       │
├─────────┼──────────┼──────────────────────────────────────┤
│ PE0~PE15│ GPIO_OUT │ 继电器1~16（推挽输出，高=吸合）        │
└─────────┴──────────┴──────────────────────────────────────┘
```

### 外部芯片

| 芯片       │ 数量 │ 接口         │ 功能                         │
|-----------|------|-------------|------------------------------|
| SP3485    │ 1    │ USART1      │ RS485 收发器                  |
| W25Q64    │ 1    │ SPI1        │ 8MB Flash，掉电保存参数       |
| BL8025    │ 1    | I2C（软件） | RTC 时钟芯片                  |
| 74HC4067  │ 2    │ GPIO        | 16选1模拟开关，复用ADC通道    |
| ULN2803   │ 4    │ GPIO        │ 达林顿管阵列，驱动继电器线圈  |

### 74HC4067 地址线映射

PCB 布线导致地址线非顺序连接，需要查表映射：

**U1（1~16路，地址线连PC0~PC3）**：

```
通道  → PC3 PC2 PC1 PC0 → 写入值
1     →  0   0   0   0  → 0x00
2     →  1   0   0   0  → 0x08
3     →  0   1   0   0  → 0x04
4     →  1   1   0   0  → 0x0C
5     →  0   0   1   0  → 0x01
6     →  1   0   1   0  → 0x09
7     →  0   1   1   0  → 0x05
8     →  1   1   1   0  → 0x0D
9     →  0   0   0   1  → 0x02
10    →  1   0   0   1  → 0x0A
11    →  0   1   0   1  → 0x06
12    →  1   1   0   1  → 0x0E
13    →  0   0   1   1  → 0x03
14    →  1   0   1   1  → 0x0B
15    →  0   1   1   1  → 0x07
16    →  1   1   1   1  → 0x0F
```

**U2（17~32路，地址线连PC4~PC7）**：映射规则同上，值左移4位（PC4~PC7）。

---

## 三、软件架构

### 目录结构

```
USER/
  main.c              程序入口，初始化+主循环
  stm32f4xx_it.c      中断处理
  system_stm32f4xx.c  系统时钟168MHz

SYSTEM/
  delay/              延时函数
  sys/                位带操作、时钟配置
  usart/              USART1底层收发

HARDWARE/
  RS485/              通信协议（命令解析、响应组装）
  Relay/              继电器控制（GPIO输出+自动关闭判断）
  Current/            电流检测（74HC4067选通+ADC采样+RMS+状态判断）
  BL8025/             RTC时钟（时间读写+计时）
  W25QXX/             Flash存储（参数读写+掉电保存）
  ADC3/               ADC3驱动（12位单次转换）
  SPI/                SPI1驱动
  IIC/                软件I2C驱动
  CRC/                CRC16查表法
  TIMER/              TIM3 1ms系统时基
  KEY/                门禁检测
  ACPowerDetection/   交流断电检测
  WATCHDOG/           外部看门狗
  LED/                LED（预留，未使用）
```

### 主循环

```c
int main(void)
{
    // 初始化（顺序重要）
    NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);
    delay_init(168);
    TIM3_Int_Init(4999, 8399);       // 1ms中断
    KEY_Init();
    Relay_GPIO_Init();
    Current_Init();
    ACPowerDetection_GPIO_Init();
    W25QXX_Init();
    SysPara_Init();                  // 从Flash读取参数
    RS485_Init();
    BL8025_Init();
    WatchDogGPIO_Init();
    if(看门狗使能) WatchDogGPIO_Enable();

    while(1)
    {
        BL8025_Process();             // 1. RTC时间+计时
        ACPowerDetection_Process();   // 2. 交流断电检测
        Key_Process();                // 3. 门禁检测
        RS485_Process();              // 4. 通信协议处理
        Relay_Process();              // 5. 继电器判断+输出
        WatchDog_Feed();              // 6. 喂狗
        Current_Process();            // 7. 电流检测
        W25QXX_Process();             // 8. Flash写入
    }
}
```

---

## 四、模块详细设计

### 模块1：TIMER（系统时基）

**文件**：`HARDWARE/TIMER/timer.c`

**功能**：TIM3 产生 1ms 定时中断，作为全系统时基。

**接口**：

```c
void TIM3_Int_Init(uint16_t arr, uint16_t psc);  // 初始化，arr=4999, psc=8399 → 1ms
```

**中断回调**：每 1ms 调用以下函数（在其他模块中实现）：

```c
USART_Timer_Ms();               // 串口接收超时
BL8025_TimerMS();               // RTC 1秒计时
ACPowerDetection_Timer_Ms();    // 交流检测10秒计时
Current_Timer_Ms();             // 电流检测1秒计时
```

**依赖**：STM32 标准外设库 TIM 驱动

---

### 模块2：USART（串口底层）

**文件**：`SYSTEM/usart/usart.c`

**功能**：USART1 底层收发，配合 RS485 方向控制。

**接口**：

```c
void uart_init(uint32_t bound);       // 初始化USART1，指定波特率
void USART_Timer_Ms(void);            // 1ms调用，10ms超时判帧结束
void USART_Send_Char(uint8_t ch);     // 发送单字节（自动切换RS485方向）
void USART_Send_Buf(uint8_t *buf, uint8_t len);  // 发送缓冲区
```

**全局变量**：

```c
uint8_t  USART_RX_BUF[USART_REC_LEN];  // 接收缓冲区
uint16_t USART_RX_STA;                  // 接收状态（bit15=完成，bit14=帧头已收）
uint16_t Rcv_Length;                    // 已接收长度
uint16_t Rcv_Time_Ms;                   // 接收超时计时器
```

**帧接收状态机**（中断中执行）：

```
初始 → 收到0x7E → 帧头标志置位 → 逐字节存入缓冲区 → 10ms超时 → 标记接收完成
```

**RS485 方向控制**：

```c
#define RS485_Recv_En()  GPIO_ResetBits(GPIOA, GPIO_Pin_8)  // PA8=0，接收
#define RS485_Send_En()  GPIO_SetBits(GPIOA, GPIO_Pin_8)    // PA8=1，发送
```

发送时：先切发送 → 发数据 → 等TC标志 → 切回接收。

**依赖**：RS485.h（方向控制宏）

---

### 模块3：CRC（校验）

**文件**：`HARDWARE/CRC/CRC.c`

**功能**：Modbus CRC16 查表法校验。

**接口**：

```c
uint16_t CRC_Check(uint8_t *buf, uint16_t len);  // 返回CRC16值
```

**算法**：预置 0xFFFF，逐字节查 256 字节高位表和低位表，返回 16 位 CRC。高字节在前。

**依赖**：无

---

### 模块4：RS485（通信协议）

**文件**：`HARDWARE/RS485/RS485.c`

**功能**：充电桩与上位机的全部通信协议，命令解析和响应组装。

**接口**：

```c
void RS485_Init(void);        // 初始化GPIO+USART1
void RS485_Process(void);     // 主循环调用，协议处理主入口
```

**全局状态变量**（本模块定义，其他模块读写）：

```c
uint8_t u8_AlarmStatus;                  // 报警状态（bit7=交流断电, bit6=门禁）
uint8_t u8_RelayStatus1_8;               // 继电器1~8开闭状态
uint8_t u8_RelayStatus9_16;              // 继电器9~16开闭状态
uint8_t u8_RelayStatus17_24;             // 继电器17~24开闭状态
uint8_t u8_RelayStatus25_32;             // 继电器25~32开闭状态
uint8_t u8_RelayStatus*_Bak;             // 上一轮状态备份
uint8_t u8_RelayOverCurrentStatus*;      // 过流关闭报警
uint8_t u8_RemoveStatus*;                // 设备移除标志
uint8_t u8_RemoveStatus*_Bak;            // 移除标志备份（上报后清零）
uint8_t u8_RemoveStatus*_Qury;           // 充电器接入标志
uint8_t u8_TrickleCurrentStatus*;        // 涓流状态标志
```

**通信协议**：

帧格式：`[0x7E][地址][命令码][数据...][CRC_H][CRC_L]`

- 帧头：0x7E
- 地址：1~254 有效，0 = 广播
- CRC16：对 [地址+命令码+数据] 计算，高字节在前
- 帧结束：10ms 无新数据判定

| 命令码 | 名称     | 请求数据区           | 响应数据区                                        |
|--------|----------|---------------------|--------------------------------------------------|
| 0x81   | 查询状态 | 无                  | [报警][继电器1-8][9-16][17-24][25-32][过流4字节][移除4字节][涓流4字节] |
| 0x82   | 开继电器 | [端口号][模式][计时][最大功率] | [端口号][结果]                          |
| 0x83   | 设供电参数 | [涓流阈值][涓流时间][最大时间][防抖时间] | 回显4字节                         |
| 0x84   | 设系统参数 | [门禁使能][交流使能][看门狗使能][电流通电时间][年月日时分秒] | 回显 |
| 0x85   | 设通信参数 | [新地址][新波特率]   | [新地址]                                          |
| 0x86   | 电流检测 | [端口号]            | 异步返回：[端口号][电流值]                         |
| 0x89   | 复位     | [系统复位][RTC复位][继电器操作] | [结果]                                  |

0x82 响应结果码：0x00=成功，0x01=被占用

0x86 特殊：若继电器未开启，临时开启检测几秒后关闭，异步返回电流值。

**RS485_Process() 四大块**：

```
块1: 解析接收帧 → CRC校验 → 地址判断 → switch命令分发
块2: 强制电流检测结果发送（0x86异步响应）
块3: 充电器在线检测结果发送（0x82开继电器1秒后检测）
块4: 统一发送（算CRC + USART_Send_Buf）
```

**依赖**：usart.c, CRC.c, Current.c, Relay.c, BL8025.c, W25QXX.c

---

### 模块5：Relay（继电器控制）

**文件**：`HARDWARE/RELAY/Relay.c`

**功能**：控制 32 路继电器 GPIO 输出，判断自动关闭条件。

**接口**：

```c
void Relay_GPIO_Init(void);    // 初始化GPIOD、GPIOE为推挽输出
void Relay_Process(void);      // 主循环调用
```

**内部函数**：

```c
void Relay_Out(void);          // 状态变量 → GPIO输出（读-改-写）
void Relay_Judge(void);        // 自动关闭判断
```

**硬件映射**：

```
继电器1~8   → PE0~PE7    （u8_RelayStatus1_8 的 bit0~bit7）
继电器9~16  → PE8~PE15   （u8_RelayStatus9_16 的 bit0~bit7）
继电器17~24 → PD0~PD7    （u8_RelayStatus17_24 的 bit0~bit7）
继电器25~32 → PD8~PD15   （u8_RelayStatus25_32 的 bit0~bit7）
```

**Relay_Out() 逻辑**：遍历 32 路，对每路做"读 GPIO 当前值 → 修改目标位 → 写回"。同一端口的 16 个引脚分高低 8 位两组独立操作，避免互相覆盖。

**Relay_Judge() 逻辑**：遍历 32 路**已开启**的继电器，按优先级检查 4 个关闭条件：

| 优先级 | 条件       | 判断依据                                | 额外操作            |
|--------|-----------|-----------------------------------------|--------------------|
| 1      | 设备移除   | `u8_RemoveStatus` 对应 bit 为 1         | 7 个关联变量清零    |
| 2      | 过流保护   | `电流值/100 >= 最大供电功率`              | 清零 + 标记过流     |
| 3      | 计时超时   | 计时模式 + `运行时间 >= 设定时间`         | 清零               |
| 4      | 自停超时   | 自停模式 + (`运行时间>=最大供电时间` 或 `涓流时间>=涓流供电时间`) | 清零 |

关闭时必须清零的 7 个关联变量：

```
u8_RelayStatus          继电器开闭
u8_RelayWorkRunTimeBuf  运行时间
u8_TrickleCurrentTimeBuf 涓流时间
u8_TrickleCurrentStatus  涓流状态
u8_RemoveStatus         移除标志
u8_RelayRemoveCntBuf    移除确认计数
u8_JitterRunTimeBuf     防抖计时
u16_RelayCurrentBuf     电流值
```

**强制电流检测**（Relay_Judge 末尾）：
0x86 命令需要对未开启的端口测电流。逻辑：临时置位 RelayStatus → Relay_Out 开继电器 → 等待通电时间 → 清除 RelayStatus → 通知 RS485 发结果。

**Relay_Process() 执行顺序**：
1. Relay_Judge() — 判断并修改状态变量
2. Relay_Out() — 状态变量写入 GPIO
3. 同步状态到 Flash 缓冲区
4. 备份当前状态（供 0x82 判断占用）

**依赖**：RS485.h（状态变量）, Current.h, BL8025.h, W25QXX.h

---

### 模块6：Current（电流检测）

**文件**：`HARDWARE/Current/Current.c`

**功能**：通过 ADC3 + 两片 74HC4067，轮询检测 32 路充电电流，判断充电状态。

**接口**：

```c
void Current_Init(void);                              // 初始化GPIO+ADC3
void ReadSetChCurrentValue(uint8_t ch);               // 读取指定通道电流值+判断状态
void Current_Process(void);                           // 主循环入口，每1秒轮询
void Current_Timer_Ms(void);                          // 1ms时基
```

**全局变量**：

```c
uint16_t u16_RelayCurrentBuf[32];       // 32路电流值，单位0.01A
uint16_t u16_ADCBuf[100];               // ADC采样缓冲区
uint8_t  u8_RelayRemoveCntBuf[32];      // 移除确认计数
UniBitDef uniCurrentDectect;             // 检测标志位
uint8_t  u8_NeedDectCh;                 // 强制检测通道号
volatile uint16_t u16_CurrentCheckTimeMs;        // 1秒计时器
volatile uint16_t u16_CurrentForceCheckTimeS;    // 强制检测秒计数
```

**SetADCCh(ch) 逻辑**：
- ch=1~16：使能 U1，禁用 U2，按映射表设 PC0~PC3 → 信号到 ADC3_CH0
- ch=17~32：禁用 U1，使能 U2，按映射表设 PC4~PC7 → 信号到 ADC3_CH1

**ReadSetChCurrentValue(ch) 流程**：

1. SetADCCh(ch) 切换通道
2. 100 次 ADC 采样（间隔 200μs，共 20ms=1 个 50Hz 周期）
3. CalcRMS() 计算 RMS 有效值 → `u16_RelayCurrentBuf[ch-1]`
4. 状态判断：
   - 电流 < 90(0.9A) 且 防抖超时 且 不在涓流 → 标记设备移除
   - 电流 < 涓流阈值 且 已运行 且 非计时模式 → 标记涓流
   - 否则 → 清除涓流标志

**CalcRMS() 算法**：

```
1. Temp = Σ ADC[i]²
2. Temp = Temp / 100
3. Temp = sqrt(Temp) × 22（校准系数）
4. return Temp（单位0.01A）
```

**Current_Process() 逻辑**：每 1 秒遍历 32 路，只对已开启的继电器调用 ReadSetChCurrentValue。

**依赖**：ADC3.c, delay.c, RS485.h, BL8025.h, W25QXX.h

---

### 模块7：ADC3（ADC 驱动）

**文件**：`HARDWARE/ADC3/adc3.c`

**功能**：ADC3 12 位单次转换驱动。

**接口**：

```c
void Adc3_Init(void);                   // 初始化ADC3
uint16_t Get_Adc3(uint8_t ch);          // 读取指定通道ADC值，返回0~4095
```

**配置**：
- 12 位分辨率，右对齐
- 独立模式，非扫描，单次转换
- 软件触发
- 480 周期采样时间
- ADC 时钟 = APB2/4 = 21MHz

---

### 模块8：SPI（SPI 驱动）

**文件**：`HARDWARE/SPI/spi.c`

**功能**：SPI1 主机模式驱动，用于 W25Q64 Flash 通信。

**接口**：

```c
void SPI1_Init(void);                   // 初始化SPI1
void SPI1_SetSpeed(uint8_t SPI_BaudRatePrescaler);  // 设置速度
uint8_t SPI1_ReadWriteByte(uint8_t TxData);         // 读写一字节
```

**配置**：
- Mode3（CPOL=High, CPHA=2Edge）
- 8 位数据，MSB 优先
- PA5=SCK, PA6=MISO, PA7=MOSI

---

### 模块9：W25QXX（Flash 存储）

**文件**：`HARDWARE/W25QXX/w25qxx.c`

**功能**：驱动 W25Q64 Flash，掉电保存系统参数和运行状态。

**接口**：

```c
void W25QXX_Init(void);                                    // 初始化
void W25QXX_Read(uint8_t *buf, uint32_t addr, uint16_t len);   // 读取
void W25QXX_Write(uint8_t *buf, uint32_t addr, uint16_t len);  // 写入（内含擦除）
void W25QXX_Erase_Sector(uint32_t addr);                   // 擦除4KB扇区
void W25QXX_Process(void);                                 // 主循环，检测变化自动写入
```

**存储布局**（联合体 FlashDataBufDef，共约 150 字节）：

| 偏移 | 字段 | 类型 | 说明 | 默认值 |
|------|------|------|------|--------|
| 0 | u8_FirstPowerOn | uint8_t | 首次上电标志 | 0xFF→0xAA |
| 1 | u8_DeviceAddress | uint8_t | 设备地址 | 1 |
| 2 | u8_CurrentBRT | uint8_t | 波特率编号 | 2(9600) |
| 3 | u8_BL8025TInit | uint8_t | RTC初始化标志 | — |
| 4 | u8_TrickleDetectionThreshold | uint8_t | 涓流检测阈值(0.1A) | 13 |
| 5 | u8_TricklePowerSupplyTime | uint8_t | 涓流供电时间(0.5h) | 2 |
| 6 | u8_MaxPowerSupplyTime | uint8_t | 最大供电时间(0.5h) | 14 |
| 7 | u8_JitterTime | uint8_t | 防抖时间(秒) | 3 |
| 8 | u8_KeyAlarmEnable | uint8_t | 门禁报警使能 | 1 |
| 9 | u8_ACPowerOutagesAlarmEnable | uint8_t | 交流断电报警使能 | 1 |
| 10 | u8_WatchDogEnable | uint8_t | 看门狗使能 | 1 |
| 11 | u8_CurrentDetectionPowerTime | uint8_t | 电流检测通电时间(秒) | 2 |
| 12 | u8_MaxPowerSupply | uint8_t | 最大供电功率(0.1A) | 14 |
| 13~16 | u8_RelayStatusBuf[4] | uint8_t | 继电器状态 | 0 |
| 17~48 | u8_RelayWorkModeBuf[32] | uint8_t | 32路工作模式 | — |
| 49~80 | u8_RelayWorkSetTimeBuf[32] | uint8_t | 32路计时设定 | — |
| 81~112 | u8_RelayWorkRunTimeBuf[32] | uint8_t | 32路运行时间 | 0 |

**存储地址**：`FLASH_SIZE - 200`（接近 8MB 末尾）

**W25QXX_Process() 逻辑**：比较当前数据与备份数据，有变化则擦除扇区+写入。

**依赖**：SPI.c

---

### 模块10：BL8025（RTC 时钟）

**文件**：`HARDWARE/BL8025/BL8025.c`

**功能**：通过 I2C 驱动 BL8025 RTC 芯片，提供日历时间和充电计时。

**接口**：

```c
void BL8025_Init(void);                  // 初始化I2C+读取时间
void BL8025ReadDateTime(void);           // 读取年月日时分秒
void BL8025SetDate(uint8_t y, uint8_t m, uint8_t d, uint8_t w);  // 设置日期
void BL8025SetTime(uint8_t h, uint8_t m, uint8_t s);             // 设置时间
void BL8025_Process(void);               // 主循环，1秒执行一次
void BL8025_TimerMS(void);               // 1ms时基
```

**I2C 地址**：0x64

**BL8025_Process() 逻辑**（每 1 秒执行）：

1. 读取 RTC 时间
2. 对已开启继电器做**防抖计时**（秒级）：电流 < 90 则抖动计时 +1，否则清零
3. 对已开启继电器做**运行时间计时**（分钟级）：每 30 分钟为一个计时单位
4. 对涓流状态继电器做**涓流时间计时**

**全局变量**：

```c
uint8_t Current_Hour/Minute/Second;       // 当前时间
uint8_t Current_Year/Month/Day/Week;      // 当前日期
uint8_t u8_TrickleCurrentTimeBuf1_8[8];   // 1~8路涓流时间
uint8_t u8_TrickleCurrentTimeBuf9_16[8];  // 9~16路涓流时间
uint8_t u8_TrickleCurrentTimeBuf17_24[8]; // 17~24路涓流时间
uint8_t u8_TrickleCurrentTimeBuf25_32[8]; // 25~32路涓流时间
uint8_t u8_JitterRunTimeBuf[32];          // 32路防抖计时
```

**依赖**：myiic.c, delay.c

---

### 模块11：IIC（软件 I2C）

**文件**：`HARDWARE/IIC/myiic.c`

**功能**：软件模拟 I2C，驱动 BL8025。

**接口**：

```c
void IIC_Init(void);                     // 初始化PB6(SCL)+PB7(SDA)
void IIC_Start(void);                    // 起始信号
void IIC_Stop(void);                     // 停止信号
void IIC_Ack(void);                      // 发送ACK
void IIC_NAck(void);                     // 发送NACK
void IIC_Send_Byte(uint8_t byte);        // 发送一字节
uint8_t IIC_Read_Byte(unsigned char ack);// 读取一字节
```

---

### 模块12：ACPowerDetection（交流断电检测）

**文件**：`HARDWARE/ACPowerDetection/ACPowerDetection.c`

**功能**：检测交流电是否正常。

**接口**：

```c
void ACPowerDetection_GPIO_Init(void);   // PA4输入初始化
void ACPowerDetection_Process(void);     // 主循环，每10秒检测
void ACPowerDetection_Timer_Ms(void);    // 1ms时基
```

**逻辑**：PA4 低电平（光耦不导通）→ 延时 10ms 去抖 → 仍为低 → 置 `u8_AlarmStatus` 的 bit7。

---

### 模块13：KEY（门禁检测）

**文件**：`HARDWARE/KEY/key.c`

**功能**：检测箱门状态。

**接口**：

```c
void KEY_Init(void);                     // PB0输入初始化
void Key_Process(void);                  // 主循环检测
```

**逻辑**：PB0 低电平 → 门被打开 → 置 `u8_AlarmStatus` 的 bit6。

---

### 模块14：WatchDog（外部看门狗）

**文件**：`HARDWARE/WATCHDOG/WatchDog.c`

**功能**：驱动外部硬件看门狗，程序跑飞时自动复位。

**接口**：

```c
void WatchDogGPIO_Init(void);           // PA2(使能)+PA3(喂狗)初始化
void WatchDogGPIO_Enable(void);         // PA2=低，使能看门狗
void WatchDogGPIO_Disable(void);        // PA2=高，禁用看门狗
void WatchDog_Feed(void);               // 翻转PA3，喂狗
```

**依赖**：无

---

## 五、模块间数据流

### 核心状态变量流

```
RS485命令(0x82) → u8_RelayStatus* ← Relay_Judge()自动清除
                       │
                       ├→ Relay_Out() → GPIO → 继电器物理动作
                       ├→ Current_Process() → 判断是否需要测电流
                       ├→ W25QXX_Process() → Flash保存
                       └→ RS485响应(0x81) → 上位机

Current_Process() → u16_RelayCurrentBuf → Relay_Judge()读取判断
                 → u8_RemoveStatus      → Relay_Judge()读取判断
                 → u8_TrickleCurrentStatus → Relay_Judge()读取判断

BL8025_Process() → u8_RelayWorkRunTimeBuf → Relay_Judge()读取判断
                → u8_TrickleCurrentTimeBuf → Relay_Judge()读取判断
                → u8_JitterRunTimeBuf      → Current_Process()读取判断

ACPower/Key → u8_AlarmStatus → RS485响应(0x81)上报
```

### 设计原则

**变量是唯一真相，GPIO 是变量的影子**。所有模块只修改状态变量，Relay_Out() 负责将变量同步到 GPIO。任何模块不得直接操作继电器 GPIO。

---

## 六、关键算法

### 6.1 RMS 电流计算

```
采样：100次ADC，间隔200μs，覆盖1个50Hz周期
计算：RMS = sqrt(Σ ADC[i]² / 100) × 22（校准系数）
单位：0.01A（如返回200 = 2.00A）
```

### 6.2 设备移除判断

```
条件：电流 < 90(0.9A) 且 防抖计时 > 设定值(默认3秒) 且 不在涓流状态
确认：移除计数 > 0 即判定（CHECK_READ_TIMES=0）
结果：置 u8_RemoveStatus → Relay_Judge 关闭继电器
```

### 6.3 涓流判断

```
条件：电流 < 涓流阈值×10(默认130=1.3A) 且 运行时间>0 且 非计时模式
结果：置 u8_TrickleCurrentStatus → 涓流计时启动 → 超时后 Relay_Judge 关闭
```

### 6.4 充电器在线检测

```
触发：0x82命令开继电器后
等待：1秒电流采样稳定
判断：电流 ≥ 90 → 充电器接入（置Qury标志）
      电流 < 90 → 未检测到充电器
```

---

## 七、Flash 数据结构定义

```c
typedef struct {
    uint8_t u8_FirstPowerOn;                   // 首次上电标志
    uint8_t u8_DeviceAddress;                  // 设备地址
    uint8_t u8_CurrentBRT;                     // 波特率编号
    uint8_t u8_BL8025TInit;                    // RTC初始化标志
    uint8_t u8_TrickleDetectionThreshold;      // 涓流检测阈值(0.1A)
    uint8_t u8_TricklePowerSupplyTime;         // 涓流供电时间(0.5h)
    uint8_t u8_MaxPowerSupplyTime;             // 最大供电时间(0.5h)
    uint8_t u8_JitterTime;                     // 防抖时间(秒)
    uint8_t u8_KeyAlarmEnable;                 // 门禁报警使能
    uint8_t u8_ACPowerOutagesAlarmEnable;      // 交流断电报警使能
    uint8_t u8_WatchDogEnable;                 // 看门狗使能
    uint8_t u8_CurrentDetectionPowerTime;      // 电流检测通电时间(秒)
    uint8_t u8_MaxPowerSupply;                 // 最大供电功率(0.1A)
    uint8_t u8_RelayStatusBuf[4];              // 继电器状态
    uint8_t u8_RelayWorkModeBuf[32];           // 32路工作模式
    uint8_t u8_RelayWorkSetTimeBuf[32];        // 32路计时设定
    uint8_t u8_RelayWorkRunTimeBuf[32];        // 32路运行时间
} FlashDataStrDef;

typedef union {
    uint8_t  u8_buf[sizeof(FlashDataStrDef)];
    FlashDataStrDef Str;
} FlashDataBufDef;
```

联合体的作用：既可以按字节整体读写 Flash（`u8_buf`），又可以按字段访问（`Str.xxx`）。

---

## 八、初始化顺序

```
1. NVIC 优先级分组
2. delay 初始化
3. TIM3 初始化（1ms时基）
4. KEY 初始化
5. Relay_GPIO 初始化（继电器全关）
6. Current 初始化（ADC3 + 74HC4067）
7. ACPowerDetection 初始化
8. W25QXX 初始化
9. SysPara_Init（从Flash读参数，首次则写默认值）
10. RS485 初始化（根据Flash中的波特率配置USART1）
11. BL8025 初始化（读RTC，无效则设默认时间）
12. WatchDog 初始化（根据Flash中的使能标志）
```

顺序重要原因：
- TIM3 必须在其他模块之前初始化（提供时基）
- W25QXX 必须在 SysPara_Init 之前（读Flash依赖SPI）
- SysPara_Init 必须在 RS485 之前（波特率从Flash读取）
- Relay_GPIO 必须在 Relay_Process 之前（GPIO先配好）
